{# Open this plain Jinja2 file and trigger Live Templates (Ctrl+J). #}
{# Framework-specific templates should be hidden unless context permits them. #}
<h1>{{ title }}</h1>
{% for user in users %}
  <p>{{ user.name }}</p>
{% endfor %}
