{# Register a generic assignmentTagProvider for `import_yaml` before testing this file. #}
{# Make resolveType(...) return nested fields for mysql.host, mysql.port, and redis.host. #}
{% import_yaml 'defaults-nested.yml' as imported_yaml %}

{{ imported_yaml.mysql.host }}
{{ imported_yaml.mysql.port }}
{{ imported_yaml.redis.host }}

{# Expect an undefined attribute warning here because `missing` is not part of the returned schema. #}
{{ imported_yaml.mysql.missing }}
